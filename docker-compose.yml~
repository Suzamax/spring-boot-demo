version: "3.7"
services:
  backend:
    build: .
    ports:
      - 8888:8888
    networks:
      - spring-postgres
  db:
    image: postgres
    restart: always
    volumes:
      - db-data:/var/lib/postgresql/data
    networks:
      - spring-postgres
    expose:
      - 5432
volumes:
  db-data:
networks:
  spring-postgres:
